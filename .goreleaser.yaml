version: 2
project_name: fedramp-browser

builds:
  - main: .
    binary: fedramp-browser
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - id: default
    formats: ["tar.gz"]
    name_template: "{{ .ProjectName }}-{{ .Os }}-{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: ["zip"]

checksum:
  name_template: 'checksums.txt'

sboms:
  - artifacts: archive

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^ci:'
      - Merge pull request
      - Merge branch

release:
  github:
    owner: hackIDLE
    name: fedramp-browser

brews:
  - repository:
      owner: hackIDLE
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/hackIDLE/fedramp-browser"
    description: "Terminal UI for browsing FedRAMP documentation"
    license: "MIT"
    install: |
      bin.install "fedramp-browser"
    test: |
      assert_match "FedRAMP", shell_output("#{bin}/fedramp-browser --help 2>&1", 1)

scoops:
  - repository:
      owner: hackIDLE
      name: scoop-bucket
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: bucket
    homepage: "https://github.com/hackIDLE/fedramp-browser"
    description: "Terminal UI for browsing FedRAMP documentation"
    license: "MIT"

winget:
  - name: fedramp-browser
    publisher: hackIDLE
    short_description: "Terminal UI for browsing FedRAMP documentation"
    license: MIT
    publisher_url: https://github.com/hackIDLE
    homepage: https://github.com/hackIDLE/fedramp-browser
    tags:
      - security
      - fedramp
      - compliance
      - cloud
      - tui
      - terminal
    repository:
      owner: hackIDLE
      name: winget-pkgs
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com
